# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Database connection for tests.
# Uses a separate database to avoid overwriting dev/demo data.
# Test isolation per test is ensured by IntegrationTestCase (transaction rollback).
DATABASE_HOST=db
DATABASE_PORT=3306
DATABASE_NAME=test
DATABASE_USER=db
DATABASE_PASSWORD=db
