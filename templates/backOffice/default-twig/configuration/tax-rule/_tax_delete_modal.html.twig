<div class="modal fade" id="tax-delete-modal" tabindex="-1" aria-labelledby="tax-delete-modal-title" aria-hidden="true"
     data-controller="bo-prefill-modal"
     data-bo-prefill-modal-source-trigger-value="data-tax-id"
     data-bo-prefill-modal-target-input-value="tax_id">
    <div class="modal-dialog">
        <div class="modal-content">
            <form action="{{ path('admin.configuration.taxes.delete') }}?_token={{ assignToken() }}"
                  method="post"
                  data-testid="tax-delete-form">
                <div class="modal-header">
                    <h2 class="modal-title h5" id="tax-delete-modal-title">{{ 'Delete tax'|trans }}</h2>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="{{ 'Close'|trans }}"></button>
                </div>
                <div class="modal-body">
                    <p>{{ 'Do you really want to delete this tax?'|trans }}</p>
                    <input type="hidden" name="tax_id" value="" data-bo-prefill-modal-target="input">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">{{ 'Cancel'|trans }}</button>
                    <button type="submit" class="btn btn-danger" data-testid="tax-delete-submit">
                        <i class="bi bi-trash" aria-hidden="true"></i> {{ 'Delete'|trans }}
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
