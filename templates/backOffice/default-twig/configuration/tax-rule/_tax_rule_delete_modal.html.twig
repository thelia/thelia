<div class="modal fade" id="tax-rule-delete-modal" tabindex="-1" aria-labelledby="tax-rule-delete-modal-title" aria-hidden="true"
     data-controller="bo-prefill-modal"
     data-bo-prefill-modal-source-trigger-value="data-tax-rule-id"
     data-bo-prefill-modal-target-input-value="tax_rule_id">
    <div class="modal-dialog">
        <div class="modal-content">
            <form action="{{ path('admin.configuration.taxes-rules.delete') }}?_token={{ assignToken() }}"
                  method="post"
                  data-testid="tax-rule-delete-form">
                <div class="modal-header">
                    <h2 class="modal-title h5" id="tax-rule-delete-modal-title">{{ 'Delete tax rule'|trans }}</h2>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="{{ 'Close'|trans }}"></button>
                </div>
                <div class="modal-body">
                    <p>{{ 'Do you really want to delete this tax rule?'|trans }}</p>
                    <input type="hidden" name="tax_rule_id" value="" data-bo-prefill-modal-target="input">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">{{ 'Cancel'|trans }}</button>
                    <button type="submit" class="btn btn-danger" data-testid="tax-rule-delete-submit">
                        <i class="bi bi-trash" aria-hidden="true"></i> {{ 'Delete'|trans }}
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
