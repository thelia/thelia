{% extends '@BackOfficeDefaultTwig/base.html.twig' %}

{% block title %}{{ 'Attributes'|trans }}{% endblock %}
{% block body_testid %}configuration-attributes{% endblock %}

{% block breadcrumb %}
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('admin.home') }}">{{ 'Home'|trans }}</a></li>
            <li class="breadcrumb-item"><a href="{{ path('admin.configuration.index') }}">{{ 'Configuration'|trans }}</a></li>
            <li class="breadcrumb-item active" aria-current="page">{{ 'Attributes'|trans }}</li>
        </ol>
    </nav>
{% endblock %}

{% block content %}
    {{ safe_hook('attributes.top') }}

    <div class="container-fluid" data-testid="attributes-page">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h1 class="h3 mb-0">{{ 'Attributes management'|trans }}</h1>
            {% if is_granted('CREATE', 'admin.configuration.attribute') %}
                <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#attribute-create-modal" data-testid="attribute-create-button">
                    <i class="bi bi-plus-lg" aria-hidden="true"></i>
                    {{ 'Add a new attribute'|trans }}
                </button>
            {% endif %}
        </div>

        {{ component('BoDataTable', {
            id: 'attributes',
            columns: [
                column('id', 'ID'|trans),
                column('title', 'Title'|trans),
                column('position', 'Position'|trans, 'center'),
                column_actions('_actions', 'Actions'|trans),
            ],
            rows: rows,
            emptyMessage: 'No attribute defined yet.'|trans,
            tbodyAttributes: {
                'data-controller': 'bo-sortable',
                'data-bo-sortable-url-value': update_position_url,
                'data-bo-sortable-token-value': update_position_token,
                'data-bo-sortable-param-name-value': 'attribute_id',
            },
        }) }}
    </div>

    {{ safe_hook('attributes.bottom') }}

    <div class="modal fade" id="attribute-create-modal" tabindex="-1" aria-labelledby="attribute-create-modal-title" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                {{ form_start(create_form, { action: path('admin.configuration.attributes.create'), method: 'post', attr: { 'data-testid': 'attribute-create-form' } }) }}
                    <div class="modal-header">
                        <h2 class="modal-title h5" id="attribute-create-modal-title">{{ 'Create a new attribute'|trans }}</h2>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="{{ 'Close'|trans }}"></button>
                    </div>
                    <div class="modal-body">
                        <div class="mb-3">
                            {{ form_label(create_form.title) }}
                            {{ form_widget(create_form.title, { attr: { class: 'form-control' } }) }}
                            {{ form_errors(create_form.title) }}
                        </div>
                        <div class="form-check mb-3">
                            {{ form_widget(create_form.add_to_all, { attr: { class: 'form-check-input' } }) }}
                            {{ form_label(create_form.add_to_all, null, { label_attr: { class: 'form-check-label' } }) }}
                        </div>
                        {{ form_widget(create_form.locale) }}
                        {{ safe_hook('attribute.create-form') }}
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">{{ 'Cancel'|trans }}</button>
                        <button type="submit" class="btn btn-primary" data-testid="attribute-create-submit">
                            <i class="bi bi-check2" aria-hidden="true"></i> {{ 'Create'|trans }}
                        </button>
                    </div>
                {{ form_end(create_form) }}
            </div>
        </div>
    </div>

    <div class="modal fade" id="attribute-delete-modal" tabindex="-1" aria-labelledby="attribute-delete-modal-title" aria-hidden="true"
         data-controller="bo-prefill-modal"
         data-bo-prefill-modal-source-trigger-value="data-attribute-id"
         data-bo-prefill-modal-target-input-value="attribute_id">
        <div class="modal-dialog">
            <div class="modal-content">
                <form action="{{ path('admin.configuration.attributes.delete') }}?_token={{ assignToken() }}" method="post" data-testid="attribute-delete-form">
                    <div class="modal-header">
                        <h2 class="modal-title h5" id="attribute-delete-modal-title">{{ 'Delete attribute'|trans }}</h2>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="{{ 'Close'|trans }}"></button>
                    </div>
                    <div class="modal-body">
                        <p>{{ 'Do you really want to delete this attribute?'|trans }}</p>
                        <input type="hidden" name="attribute_id" value="" data-bo-prefill-modal-target="input">
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">{{ 'Cancel'|trans }}</button>
                        <button type="submit" class="btn btn-danger" data-testid="attribute-delete-submit">
                            <i class="bi bi-trash" aria-hidden="true"></i> {{ 'Delete'|trans }}
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
