<div class="modal fade" id="brand-delete-modal" tabindex="-1" aria-labelledby="brand-delete-modal-title" aria-hidden="true"
     data-controller="bo-prefill-modal"
     data-bo-prefill-modal-source-trigger-value="data-brand-id"
     data-bo-prefill-modal-target-input-value="brand_id">
    <div class="modal-dialog">
        <div class="modal-content">
            <form action="{{ path('admin.brand.delete') }}?_token={{ assignToken() }}"
                  method="post"
                  data-testid="brand-delete-form">
                <div class="modal-header">
                    <h2 class="modal-title h5" id="brand-delete-modal-title">{{ 'Delete brand'|trans }}</h2>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="{{ 'Close'|trans }}"></button>
                </div>
                <div class="modal-body">
                    <p>{{ 'Do you really want to delete this brand?'|trans }}</p>
                    <input type="hidden" name="brand_id" value="" data-bo-prefill-modal-target="input">
                    {{ safe_hook('brand.delete-form') }}
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">{{ 'Cancel'|trans }}</button>
                    <button type="submit" class="btn btn-danger" data-testid="brand-delete-submit">
                        <i class="bi bi-trash" aria-hidden="true"></i> {{ 'Delete'|trans }}
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
