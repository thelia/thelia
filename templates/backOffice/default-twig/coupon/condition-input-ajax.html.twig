{{ inputs_drawn|default('')|raw }}
<input name="conditionIndex" type="hidden" value="{{ condition_index|default('') }}">
<input name="conditionServiceId" type="hidden" value="{{ condition_service_id|default('') }}">
